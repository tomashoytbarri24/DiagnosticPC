# V244 — Square Trends Container

Cambio único: el contenedor que encapsula los tres gráficos CPU/RAM/GPU pasa a `corner_radius=0` en constructor, dashboard y autoridad responsive. No se modifican tamaños, tipografías, series, telemetría ni almacenamiento.
